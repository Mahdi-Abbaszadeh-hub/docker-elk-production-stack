# config/logstash.conf
input {
  tcp {
    port => 5000
    codec => json
  }
}

filter {
  mutate {
    add_tag => ["elk-stack"]
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    user => "elastic"
    password => "YOUR_ELASTIC_PASSWORD"
    index => "logstash-%{+YYYY.MM.dd}"
    ssl_enabled => false
  }
}